import { FieldValidateDirective } from './field-validate.directive';

describe('FieldValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldValidateDirective();
    expect(directive).toBeTruthy();
  });
});
