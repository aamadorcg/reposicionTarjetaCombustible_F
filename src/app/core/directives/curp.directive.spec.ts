import { CurpDirective } from './curp.directive';

describe('CurpDirective', () => {
  it('should create an instance', () => {
    const directive = new CurpDirective();
    expect(directive).toBeTruthy();
  });
});
